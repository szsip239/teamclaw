# ── Web ──────────────────────────
fastapi
uvicorn[standard]
python-multipart

# ── DB ───────────────────────────
asyncpg

# ── AI services (OpenAI-compatible HTTP) ──
openai

# ── Document parsing ─────────────
PyMuPDF
Pillow
openpyxl
xlrd

# ── Chinese tokenization (for FTS write/query symmetry) ──
jieba

# ── Utilities ────────────────────
python-dotenv
requests
httpx
numpy
